import Link from "next/link";
import { requirePageAuth } from "@/lib/auth";
import { getRepository } from "@/lib/repository";
export const dynamic = "force-dynamic";
export default async function ProviderOpportunitiesPage(): Promise<React.JSX.Element> { const auth = await requirePageAuth(["organization"]); const opportunities = await (await getRepository()).listOpportunities({ organizationId: auth.organizationId ?? undefined }); return <main className="page-shell"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Provider tools</p><h1 className="mt-2 text-4xl font-black">Your opportunities</h1><p className="mt-2 text-ink/60">One simple view from review to closing day.</p></div><Link className="button" href="/dashboard/opportunities/new">Post an opportunity</Link></div><div className="mt-8 space-y-3">{opportunities.map((item) => <article className="card flex flex-wrap items-center justify-between gap-4" key={item.id}><div><h2 className="text-lg font-black">{item.title}</h2><p className="mt-1 text-sm capitalize text-ink/60">{item.verificationStatus} · {item.status} · {item.workMode}</p></div><div className="text-right"><span className="pill">Closes {item.deadline.toLocaleDateString("en-UG")}</span><p className="mt-2 text-xs text-ink/50">Checked {item.checkedAt.toLocaleDateString("en-UG")}</p></div></article>)}</div></main>; }

import { expect, test } from "@playwright/test";
test("monetization remains visibly gated while discovery stays free", async ({ page }) => { const errors: string[] = []; page.on("console", (message) => { if (message.type() === "error") errors.push(message.text()); }); await page.goto("/dashboard/monetization"); await expect(page.getByRole("heading", { name: "Monetization readiness" })).toBeVisible(); await expect(page.getByText("Activation safely blocked")).toBeVisible(); await expect(page.getByText("Always free for opportunity seekers")).toBeVisible(); await page.goto("/feed"); await expect(page.getByRole("heading", { name: "Matches worth your time." })).toBeVisible(); await expect(page.getByRole("link", { name: "View match" }).first()).toBeVisible(); expect(errors).toEqual([]); });
